import featureDetect from 'dasht/utils/feature-detect';

module('featureDetect');

// Replace this with your real tests.
test('it works', function() {
  var result = featureDetect();
  ok(result);
});

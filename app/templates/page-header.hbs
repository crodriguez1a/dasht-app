<div class="page-actions">
  <a href="#" {{action 'toggleFilters'}} {{bind-attr class=":actionable controller.currentlyFiltered:active"}}>
    {{#if controller.filtering}}
      {{fa-icon "fa-close"}}
    {{else}}
      {{fa-icon "fa-filter"}}
    {{/if}}
  </a>
  {{#link-to 'dashtboard' classNames="actionable"}}{{fa-icon "fa-close"}}{{/link-to}}
</div>

{{#if controller.filtering}}
  {{live-filtering model=this}}
{{/if}}

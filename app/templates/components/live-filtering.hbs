<div class="filtering">
  <ul>
    {{#each filter in filtersArr}}
      <li class="actionable" {{action 'toggleFilter' filter.name}}>{{check-box filter.on}} {{filter.name}}</li>
    {{/each}}
  </ul>
</div>
